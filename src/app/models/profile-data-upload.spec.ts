import { ProfileDataUpload } from './profile-data-upload';

describe('ProfileDataUpload', () => {
  it('should create an instance', () => {
    expect(new ProfileDataUpload()).toBeTruthy();
  });
});
