import { PhotoProfileUpload } from './photo-profile-upload';

describe('PhotoProfileUpload', () => {
  it('should create an instance', () => {
    expect(new PhotoProfileUpload()).toBeTruthy();
  });
});
